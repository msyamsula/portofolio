---
# cluster IP
apiVersion: v1
kind: Service
metadata:
  name: friend-clusterip
  labels:
    app: friend
    type: clusterip
spec:
  type: ClusterIP
  selector:
    app: friend
    type: pod
  ports:
    - protocol: TCP
      port: 10000 # service port
      targetPort: 10000 # container port
      name: friend-clusterip

---
#Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: friend-deployment
  labels:
    app: friend
    type: deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: friend
      type: pod
  template:
    metadata:
      labels:
        app: friend
        type: pod
    spec:
      containers:
      - name: friend
        image: 717420436752.dkr.ecr.ap-southeast-1.amazonaws.com/friend:cabf0e8-18
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 10000
        envFrom:
          - configMapRef:
              name: friend-env
        command: ["./main"]

