---
# cluster IP
apiVersion: v1
kind: Service
metadata:
  name: graph-canary-clusterip
  labels:
    app: graph-canary
    type: clusterip
spec:
  type: ClusterIP
  selector:
    app: graph-canary
    type: pod
  ports:
    - protocol: TCP
      port: 10000 # service port
      targetPort: 10000 # container port
      name: graph-canary-clusterip

---
#Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graph-canary-deployment
  labels:
    app: graph-canary
    type: deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graph-canary
      type: pod
  template:
    metadata:
      labels:
        app: graph-canary
        type: pod
    spec:
      containers:
      - name: graph-canary
        image: 717420436752.dkr.ecr.ap-southeast-1.amazonaws.com/graph:92482e9-2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 10000
        envFrom:
          - configMapRef:
              name: graph-env
        command: ["./main"]

