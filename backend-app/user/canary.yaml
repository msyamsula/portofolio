---
# cluster IP
apiVersion: v1
kind: Service
metadata:
  name: user-canary
  labels:
    app: user-canary
    type: clusterip
spec:
  type: ClusterIP
  selector:
    app: user-canary
    type: pod
  ports:
    - protocol: TCP
      port: 10000 # service port
      targetPort: 10000 # container port
      name: user-canary

---
#Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-canary-deployment
  labels:
    app: user-canary
    type: deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-canary
      type: pod
  template:
    metadata:
      labels:
        app: user-canary
        type: pod
    spec:
      containers:
      - name: user-canary
        image: 717420436752.dkr.ecr.ap-southeast-1.amazonaws.com/user:cabf0e8-18
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 10000
        envFrom:
          - configMapRef:
              name: user-env
        command: ["./main"]

